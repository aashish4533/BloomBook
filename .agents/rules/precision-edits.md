---
trigger: always_on
---

Do not refactor, "clean up," or modify code outside the specific files or modules requested.

If a change requires touching a dependency, ask for permission before modifying the second file.

Restrict all logic updates strictly to the requested functionality.